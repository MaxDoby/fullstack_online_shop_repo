# Admin UI Notes

1. Show opened product images in the center of the browser as a separate card-style preview, visually similar to product cards on the main products page.

2. Close opened images by clicking the background overlay, and/or add a polished close X button in the top-right corner.

3. When clicking the Images button in the admin products table, show the images directly under that product row instead of at the bottom of the full product list.

4. Bug: after uploading an image or setting an image as primary, the UI does not visibly refresh and there is no clear feedback after clicking the Set primary button.
